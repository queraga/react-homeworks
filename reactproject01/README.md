# 🌐 CoolJob - Job Search Landing Page

**CoolJob** is a responsive landing page for a job search platform, built as part of a React learning project.  
The project demonstrates component-based architecture, modular CSS styling, and dynamic rendering using the `map()` method.

---

## 🚀 Technologies
- **React (Vite)**
- **CSS Modules**
- **JavaScript (ES6+)**
- **Flexbox & Grid Layout**
- **File-based project structure**

---

## 🧩 Project Structure

```
src/
├── assets/               # Icons and images
├── components/
│   ├── header/           # Header section
│   ├── hero/             # Hero section with search form
│   ├── categories/       # Job categories section (rendered via map)
│   ├── newJob/           # “New Jobs” section with dynamic cards
│   ├── footer/           # Footer with social links
└── App.jsx               # Main component
```

---

## 💡 Key Features
- Each card (category or job) is rendered from reusable components via **`.map()`**.  
- Styling is separated by component using **CSS Modules**.  
- Layout fully matches the **Figma design** with correct spacing and shadows.  
- Easily extendable structure for future features or API integration.

---

## ⚙️ How to Run
```bash
npm install
npm run dev
```
Then open:
http://localhost:5173/

---
🧠 What I Learned
	•	How to organize a React project using component-driven development.
	•	How to manage styles using CSS Modules.
	•	How to render UI dynamically with map() and props.
	•	How to maintain clean, reusable project structure for scalability.

👤 Author
Queraga
📍 Berlin, Germany
🔗 [GitHub Profile](https://github.com/YevhenLashkov)
