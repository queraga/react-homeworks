# React Material UI Dialog App

This is a simple React application created as part of **Homework** for the course **React**.  
The goal of the project is to get familiar with **Material UI (MUI)** components and basic UI patterns in React.

---

## 📌 Description

The application includes:

- A top navigation bar (**AppBar**) with a title
- Main content wrapped in a **Container**
- A button that opens a modal dialog
- A **Dialog** component with text and action buttons

The project demonstrates:

- State management using `useState`
- Opening and closing a dialog window
- Basic usage of Material UI components

---

## 🛠 Technologies Used

- React
- Material UI (MUI)
- JavaScript (ES6)
- CSS
